#ifndef STRING_H
#define STRING_H
#include "types.h"
//simple function to show stringlength
uint16 strlength(string ch)
{
    uint16 i =1;
    while (ch[i++])
    {
        return --i;
    }
    
}
#endif